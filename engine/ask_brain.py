import sys
from main import main

"""
ask_brain.py — Brain 2 RAG Query Agent (Refactored Wrapper)
=========================================================
This script is now a wrapper around the modularized engine/main.py.
It maintains backwards compatibility for CLI aliases.
"""

if __name__ == "__main__":
    main()
